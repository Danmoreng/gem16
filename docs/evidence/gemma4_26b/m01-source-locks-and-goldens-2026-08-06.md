# Gemma 4 26B M01 source-lock and golden handoff

Date: 2026-08-06

Branch: `feat/26b-m01-source-locks`

M01 parent: `3bf7b6e4427433ae766fe9238ed6d8c991398b6b`

Status: accepted by the owner on 2026-08-06; every explicit M01 exit criterion and the direct Unsloth
token-reference objective pass with disclosed diagnostic limitations

No compiler, production runtime, CUDA kernel, CMake target or 12B model behavior changed in M01.

## Immutable source locks

All selected files were downloaded or imported into the shared Hugging Face blob cache, linked into checkpoint
views without payload copies and freshly re-hashed with `tools/fetch_model.py --verify-only`.

| Source | Revision | Locked bytes | Lock SHA-256 |
|---|---|---:|---|
| Google QAT BF16 | `f1e06dc520982d9b9edd76859fdb7ab209449949` | 51,644,341,801 | `3d9441fdebef33785e33181c335338208b8bf868cb8c7da692fd9c765cca8230` |
| Google ordinary BF16 | `4d7ae4984b7db7de8f8457170b3f1a419ee76d52` | 51,644,340,088 | `1932b06d568ce8c72f3d2013d39ffd15dd61d37732638c2013f2220eb177a620` |
| Google official Q4_0 | `d1c082be9cf3c8a514acf63b8761f4b41935842e` | 15,634,222,799 | `0893ac0a4db32f0c5549bc17b92f3951ab4da1a6180b188094190a4bbfc88784` |
| Unsloth NVFP4 | `20df0542b1a86ce19f495ac2eca2c7c12bce82f9` | 16,942,099,391 | `6635b15f4f974dec22821c7a9efd4d2404ba4ce9afb1424ca2f3529d20db7941` |

Every large file records content SHA-256, Git blob identity, LFS identity and Xet hash. All repository revisions are
full immutable commits. The software-reference lock SHA-256 is
`577528f96ac1e64d5e258a9a50eaa60f4cb48bf0294b8c6db96bace1f921ab6e`; it pins Transformers 5.14.1, vLLM 0.26.0,
FlashInfer 0.6.14, compressed-tensors 0.17.0, CUTLASS 4.5.2, llama.cpp b10240, imp, Accelerate 1.14.0,
Safetensors 0.8.0, PyTorch 2.11.0 and huggingface_hub 1.26.0 to full source commits.

A fresh final verification read all 135,865,004,079 locked source bytes successfully. Verification took 29 seconds
for QAT BF16, 30 seconds for ordinary BF16, 9 seconds for official Q4_0 and 10 seconds for Unsloth NVFP4 on the
reference filesystem.

## Storage and host-memory evidence

The cache is authoritative. Checkpoint views use hardlinks, including nested files; the pre-existing official text
Q4_0 payload is the same inode as its verified cache blob. No source checkpoint payload is tracked by Git. After
all sources and M01 evidence, the filesystem has 167,519,563,776 bytes (156.015 GiB) available.

The bounded QAT-BF16 diagnostic load/forward used at most 12 GiB GPU placement and 38 GiB CPU placement. The final
capture records:

- 25,233,141,760 parameters;
- approximately 49 GiB maximum process RSS;
- 11,249,020,416 peak CUDA-allocated bytes;
- device-map counts of 7 GPU, 23 CPU and 4 disk-designated modules;
- no offload files produced;
- explicit diagnostic/non-performance status.

This proves that the selected tiny BF16 reference capture is feasible on this 62 GiB/no-swap host, but it does not
qualify CPU offload, expert streaming or any nonresident path for product or performance use.

## Source inventory and comparison

All four source inventories regenerate byte-for-byte from their checked tools and locked inputs:

| Source | Tensor count | Payload/file bytes | Storage summary |
|---|---:|---:|---|
| QAT BF16 | 1,013 | 51,611,872,412 payload | 1,013 BF16 |
| ordinary BF16 | 1,013 | 51,611,872,412 payload | 1,013 BF16 |
| Unsloth NVFP4 | 47,478 | 16,903,408,612 payload | 923 BF16, 23,220 F32, 11,725 FP8, 11,610 U8 |
| official Q4_0 | 658 | 14,439,363,584 file | 392 F32, 265 Q4_0, 1 Q6_K |

[`source-audit.json`](../../../benchmarks/goldens/gemma4_26b/source-audit.json) is generated by
`tools/audit_gemma4_26b_sources.py` and passes:

- QAT and ordinary BF16 have exactly the same 1,013 tensor names, dtypes, shapes and byte lengths; only the outer
  `transformers_version` config field differs, while their locked weight-shard hashes differ;
- QAT, ordinary and Unsloth have byte-equivalent `text_config` objects;
- Unsloth declares compressed-tensors mixed precision, 115 FP8 attention weights and 11,610 packed NVFP4 target
  weights with their local/global scales;
- official Q4_0 metadata exactly matches the BF16 layer count, context, hidden/intermediate dimensions, local/global
  KV-head schedule, sliding window and final-logit softcap;
- raw names across BF16, compressed-tensors and GGUF are intentionally not treated as one physical layout contract.

## Frozen corpus and tokenizer identity

The frozen corpus contains 12 prompts covering tiny input, multilingual text, code, reasoning, tool syntax,
repetition, local-window boundaries and longer spans. Calibration, development and held-out test manifests have
zero document-hash and token-span-hash overlap. The test split is labeled
`held_out_final_quality_no_quantizer_tuning`; no compiler or quantizer tuning occurred in M01.

QAT BF16 is canonical. Ordinary BF16 and official Q4_0 matched every exact token ID during corpus generation; Q4_0
used pinned weightless `llama-tokenize`, so no GGUF weights were loaded. The final source audit additionally proves
that Unsloth's tokenizer and chat template produce exactly the same rendering and token IDs for all 12 source
prompts. All tokenizer checks use local locked files with `trust_remote_code=false`.

## QAT-BF16 golden

The retained compact fixture captures two selected positions at the embedding, layers 0, 5, 6 and 29, and final
norm boundaries. It contains 88 named captures, including Q/K/V/O boundaries where present, attention context,
shared MLP, full 128-way router probabilities, selected top-8 expert IDs/weights, each routed-expert contribution,
routed sum, residual boundaries and final output.

Two independent runs match exactly after excluding only nondeterministic `max_rss_kib`; execution placement, source,
software, prompt, all captures, full final logits and generated IDs are compared. The retained identities are:

- full 262,144-element F32 final-logit file SHA-256:
  `289893e5cfef1cd44b4f6cd536d80c33183406ec4e274d483c65a97fc69c1522`;
- greedy generated IDs: `[7676, 106]`;
- repeat report: pass, 88 captures, execution metadata included except RSS peak.

The fixture is a diagnostic numerical reference, not evidence for a production 26B runtime or global tolerance.

## Official Q4_0 reference

Pinned llama.cpp b10240 loaded the official text GGUF with 20 GPU layers, one 1,024-token slot and greedy sampling.
Two runs generated `[7676, 106]` exactly. Teacher forcing at those positions also selected `[7676, 106]` as Top-1.
The sampled startup/run VRAM peak was 9,646 MiB. The evidence records the model lock, inventory, software lock,
storage types, exact device and prompt IDs and remains explicitly ineligible for performance claims.

## Unsloth direct reference and limitation

The first unbounded Unsloth vLLM attempt exhausted host RAM during FlashInfer fused-MoE JIT and required REISUB.
Commit `f901044` adds a transient-systemd wrapper with a 45 GiB hard limit, four outer jobs, one internal NVCC
thread, whole-cgroup OOM kill and descendant cleanup. The complete cause and controlled follow-up are in
[`m01-unsloth-vllm-oom-2026-08-06.md`](m01-unsloth-vllm-oom-2026-08-06.md).

The bounded cold run compiled `fused_moe_120` and all 66 autotune configurations with at most four `cicc` processes
and 35,722,186,752 cgroup bytes, but its 0.78 GPU reservation reached 14,560 MiB and was stopped. The accepted
warm-cache diagnostic reduced GPU utilization to 0.70, retained supported chunked prefill, used one explicit
warmup and completed with 11,874 MiB sampled peak VRAM and 11,222,122,496 cgroup peak bytes. The capture process
wrote its result and exited successfully; systemd subsequently timed out implicit EngineCore teardown after 30
seconds, killed the remaining control group and left no process or allocation. The checked tool now performs an
explicit pinned EngineCore shutdown.

Dispatch logs prove CUTLASS FP8 attention projections, FlashInfer CUTLASS NVFP4 GEMM/MoE and Triton attention. The
warmup and both retained runs generated `[7676, 236761]` (`"OK."`); the retained token IDs and text are exact, while
Top-20 membership and logprobs vary. This direct result is therefore a token-level diagnostic reference, not an
exact-logit oracle or performance result. It also differs at the second token from QAT BF16 and official Q4_0,
which produced `[7676, 106]`.

## Terms and distribution boundary

All four model locks record `https://ai.google.dev/gemma/terms`; the pinned model-card metadata declares
`apache-2.0` and links the Gemma 4 license. The repository contains only lock metadata and compact numerical
evidence, not model payloads. These records establish provenance, not legal advice or release approval.

Distribution, hosting or publication of a future gem16-compiled 26B checkpoint remains blocked pending explicit
owner review of the then-current source and derived-artifact obligations. The artifact must remain labeled as a
project-built gem16 derivative, never an official Google or Unsloth quantization.

## Validation

Passed on the M01 branch:

- all Python tests: 68/68;
- Host Debug configure/build and CTest: 1/1;
- Blackwell Release configure/build and CTest: 2/2;
- all four checkpoint `--verify-only` runs;
- byte-exact regeneration of all four source inventories;
- generated source audit and 12-prompt tokenizer audit;
- imported-plan package checksums: all files pass;
- Markdown relative-link audit: 164 files, 359 relative links, zero failures;
- Python byte compilation and `git diff --check`: pass.

The Blackwell build retains two pre-existing `cutlass_sm120.cu` extra-braces warnings and `setmaxnreg` compatibility
remarks; M01 changes no C++ or CUDA source.

## Exit checklist

| M01 criterion | Result | Evidence |
|---|---|---|
| Every source has a full immutable revision | PASS | four model locks and software-reference lock |
| Disk/cache and BF16 memory plan measured | PASS | 156.015 GiB currently free; bounded 49 GiB RSS diagnostic capture |
| Every required source file has size and SHA-256 | PASS | fresh verification of 135,865,004,079 bytes |
| Reference tokenization is identical | PASS | 12/12 QAT, ordinary, Q4_0 and Unsloth source prompts |
| Layers 0, later local/global layers and final logits captured | PASS | selected layers 0, 5, 6 and 29 plus final norm/logits |
| Router includes all 128 probabilities and selected IDs/weights | PASS | two positions at each selected layer |
| Calibration and held-out test are disjoint | PASS | frozen split audit, zero overlaps |
| Final test was not used for quantizer tuning | PASS | no compiler/quantizer implementation exists in M01 |
| Direct Unsloth runtime evidence is captured | PASS with limitation | repeated token/text identity; non-exact Top-20/logprobs; diagnostic offload only |

The owner accepted this M01 handoff on 2026-08-06. M02 model configuration/static traits is unblocked and must
start from the final M01 closure commit on separate branch `feat/26b-m02-model-traits`; M03 and later work remains
blocked. Distribution of a future derived checkpoint remains separately blocked pending explicit owner review.
